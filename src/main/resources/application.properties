spring.application.name=backend

# 1. DataSource (SQLite 설정)
# project_db.db 파일은 backend 폴더에 생성됩니다.
# 프로젝트 루트(DataBase) 기준으로 backend/project_db.db를 사용합니다.
# 주의: 애플리케이션은 프로젝트 루트에서 실행되어야 합니다.
spring.datasource.url=jdbc:sqlite:backend/project_db.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# 2. JPA/Hibernate ??
# ?? ??? ?????? ?? Dialect ??? ??? ?????.
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# DB ??? ?? ?? (?? ? ??? ???? ??? ??/????)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 3. Swagger / OpenAPI (Springdoc)
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html